const ids = {
  PRODUCTION: 'production',
  TEST: 'test',
};

const environmentConstants = {
  [ids.PRODUCTION]: {
    endpoint: 'frontend-config',
    suffix: '',
  },
  [ids.TEST]: {
    endpoint: 'frontend-config-test/test/common.json',
    suffix: 'test',
  },
};

const testEnvironmentLab = 'develop-nordic';

module.exports = {
  ids,
  environmentConstants,
  testEnvironmentLab,
};
