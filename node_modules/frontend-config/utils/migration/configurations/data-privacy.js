const config = require('data-privacy-config');
const { platforms } = require('../../../config/platforms');

module.exports = {
  config,
  rules: [{
    platforms: platforms.filter(platformId => platformId !== 'TI'
      && platformId !== 'TM'
      && platformId !== 'TL'
      && platformId !== 'BC').map(id => ({ id })),
    keys: [{
      key: 'cookieDisclaimer',
    }, {
      key: 'cookiesConsentBanner',
    }, {
      key: 'httpCookies',
    }],
  }],
};
