const objectMenuMapper = value =>
  Object.values(value || {})
    .reduce((groups, group) => groups.concat(group), [])
    .reduce((items, item) => items.concat(item), [])
    .map(key => `url.${key}`)

module.exports = {
  objectMenuMapper,
};
