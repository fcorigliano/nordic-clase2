const footerAccessMapper = value => ((value || {}).items || [])
  .reduce((groupTempKeys, group) => groupTempKeys.concat(group.items
    .reduce((itemsTempKeys, item) => itemsTempKeys.concat(`url.${item.link}`), [])), []);

module.exports = {
  footerAccessMapper,
};
