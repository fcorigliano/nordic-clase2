const { getOverrides } = require('./get-overrides');
const { writeFileSync } = require('fs');
const { join } = require('path');

writeFileSync(join(__dirname, '../../config/overrides.json'), JSON.stringify(getOverrides(), null, 2));
