const mod = require('../../../building_blocks/presets/widget');

describe('building_blocks/presets/widget.js', () => {
  test('should re-export a module', () => {
    expect(mod).toBeDefined();
  });
});
