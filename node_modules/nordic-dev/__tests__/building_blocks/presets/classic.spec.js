const mod = require('../../../building_blocks/presets/classic');

describe('building_blocks/presets/classic.js', () => {
  test('should re-export a module', () => {
    expect(mod).toBeDefined();
  });
});
