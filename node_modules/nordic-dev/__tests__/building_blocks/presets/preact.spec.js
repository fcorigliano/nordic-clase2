const mod = require('../../../building_blocks/presets/preact');

describe('building_blocks/presets/preact.js', () => {
  test('should re-export a module', () => {
    expect(mod).toBeDefined();
  });
});
