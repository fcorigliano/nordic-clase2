const userMenuProps = (props) => {
  const { isLoggedIn, userMenu, user, navigationPIIData } = props;
  const { cbtInfo, links, sites, logisticTypes } = userMenu;
  return {
    isLoggedIn,
    cbtInfo,
    user,
    links,
    sites,
    logisticTypes,
    navigationPIIData,
  };
};

module.exports = userMenuProps;
