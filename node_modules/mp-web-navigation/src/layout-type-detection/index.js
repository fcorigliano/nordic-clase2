const types = require('./types');

const isPlus = type => type === types.PLUS;
const isHidden = type => type === types.HIDDEN;
const isClean = type => type === types.CLEAN;

module.exports = {
  isPlus,
  isHidden,
  isClean,
  types,
};
