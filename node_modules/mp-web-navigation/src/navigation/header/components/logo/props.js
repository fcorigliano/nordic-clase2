const { isHidden } = require('../../../../layout-type-detection');

const logoProps = (props) => {
  if (isHidden(props.type)) return {};

  const { urls, labels } = props;

  return {
    url: urls.baseDomain.link,
    label: labels.headTitle,
  };
};

module.exports = logoProps;
