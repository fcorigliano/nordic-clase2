const welcomeProps = require('./welcome/props');
const navigationProps = require('../navigation/props');

const loggedInProps = props => ({
  welcome: welcomeProps(props),
  navigation: navigationProps(props),
});

module.exports = loggedInProps;
