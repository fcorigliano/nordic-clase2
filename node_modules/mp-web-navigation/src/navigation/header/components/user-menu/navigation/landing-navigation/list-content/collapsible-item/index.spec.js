const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const CollapsibleItem = require('.');

const props = {
  id: 'digital_account',
  labelState: {
    open: 'Ver menos',
    closed: 'Ver más',
  },
};

describe('Mercado Pago Landing Navigation', () => {
  describe('CollapsibleItem Component', () => {
    it('Should match with the default snapshot for collapsible-item', () => {
      const renderer = new ShallowRenderer();
      const wrapper = (
        <CollapsibleItem {...props} />
      );
      expect(renderer.render(wrapper)).toMatchSnapshot();
    });
  });
});
