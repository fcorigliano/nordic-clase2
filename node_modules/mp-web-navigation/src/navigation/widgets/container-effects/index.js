const React = require('react');
const Script = require('frontend-script');
const readClientFile = require('../../utils/read-client-file');

const clientFileContent = readClientFile('effects.js');

const ContainerEffects = () => (
  <Script on="now">
    {clientFileContent}
  </Script>
);

module.exports = ContainerEffects;
