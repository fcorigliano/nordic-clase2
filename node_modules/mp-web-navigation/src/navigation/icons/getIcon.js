/* eslint-disable security/detect-object-injection */
const chevron = require('./chevron');
const logout = require('./logout');
const help = require('./help');

const getIcon = (icon) => {
  const iconMapper = {
    chevron,
    logout,
    help,
  };
  return iconMapper[icon] || null;
};

module.exports = getIcon;
