const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const ReclamationBook = require('.');

const baseProps = {
  uiNavigationVersion: '5.13.0',
  reclamationBookConfig: {
    link: 'https://www.mercadopago.com.pe/ayuda/19273',
    name: 'libro de reclamaciones',
  },
};

describe('Mercado Pago reclamationBook', () => {
  describe('Footer reclamationBook Component', () => {
    it('matches the default snapshot for footer reclamationBook', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <ReclamationBook
          {...baseProps}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
