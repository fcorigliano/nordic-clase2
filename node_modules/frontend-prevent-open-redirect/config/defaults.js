/**
 * Prevent open redirect middleware default config
 *
 * @type {Omit<import('../index').Config, 'handleInvalidRedirectParam'>}
 */
module.exports = {
  params: ['go'],
  protocols: ['meli:', 'mercadopago:', 'http:', 'https:'],
  whitelist: [],
};
