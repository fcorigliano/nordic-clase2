import { Callback } from "../../utils/callback-container";
import { Config } from "../request/config";

export interface MultipleRequest extends Config {
    args: {
        method: string,
        args: any
    }[];
    callback?: Callback;
}