export enum ErrorCodes {
    NoAppConnection = "connection_code",
    Timeout = "timeout_code",
    Native = "native_code",
    UnknownMessage = "unknown_message",
    UnavailableCommand = "unavailable_command"
}