import { MobileWebkitEngine } from "../../mobile-webkit-engine";
import { CommandBackSupport } from "../utils/command-backsupport";
import { NavigationCommands } from "./navigation_commands";
import { NavigationCommandsImpl } from "./navigation_commands_impl";
import { NavigationCommandsLegacy } from "./backsupport/navigation_commands_legacy";

export const navigationBackSupport: CommandBackSupport<NavigationCommands> = {
    [MobileWebkitEngine.Legacy]: (mobileWebKit) => new NavigationCommandsLegacy(mobileWebKit),
    [MobileWebkitEngine.V200]: (mobileWebKit) => new NavigationCommandsImpl(mobileWebKit)
};
