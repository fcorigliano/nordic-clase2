import { MobileWebkitEngine } from "../../mobile-webkit-engine";
import { CommandBackSupport } from "../utils/command-backsupport";
import { TrackingCommands } from "./tracking_commands";
import { TrackingCommandsImpl } from "./tracking_commands_impl";

export const trackingBackSupport: CommandBackSupport<TrackingCommands> = {
  [MobileWebkitEngine.Legacy]: (mobileWebKit) => new TrackingCommandsImpl(mobileWebKit),
  [MobileWebkitEngine.V200]: (mobileWebKit) => new TrackingCommandsImpl(mobileWebKit)
};