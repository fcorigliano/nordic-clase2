import { MobileWebkitEngine } from "../../mobile-webkit-engine";
import { CommandBackSupport } from "../utils/command-backsupport";
import { LifecycleCommands } from "./lifecycle_commands";
import { LifecycleCommandsImpl } from "./lifecycle_commands_impl";

export const lifecycleBackSupport: CommandBackSupport<LifecycleCommands> = {
    [MobileWebkitEngine.Legacy]: (mobileWebKit) => new LifecycleCommandsImpl(mobileWebKit),
    [MobileWebkitEngine.V200]: (mobileWebKit) => new LifecycleCommandsImpl(mobileWebKit)
};
