import { MobileWebkitEngine } from "../../mobile-webkit-engine";
import { CommandBackSupport } from "../utils/command-backsupport";
import { BarCommandsLegacy } from "./backsupport/bar_commands_legacy";
import { BarCommands } from "./bar_commands";
import { BarCommandsImpl } from "./bar_commands_impl";

export const barBackSupport: CommandBackSupport<BarCommands> = {
    [MobileWebkitEngine.Legacy]: (mobileWebKit) => new BarCommandsLegacy(mobileWebKit),
    [MobileWebkitEngine.V200]: (mobileWebKit) => new BarCommandsImpl(mobileWebKit)
}