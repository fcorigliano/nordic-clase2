import { RequestNativeEventArgs } from "../../../events/native-event";

export type Subscription = (event: CustomEvent<RequestNativeEventArgs>) => void;

export interface EventSubscriptionId {
    topic: string,
    id: string,
    subscription: Subscription;
}
