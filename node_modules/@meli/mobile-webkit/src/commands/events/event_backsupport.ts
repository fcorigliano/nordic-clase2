import { MobileWebkitEngine } from "../../mobile-webkit-engine";
import { CommandBackSupport } from "../utils/command-backsupport";
import { EventCommands } from "./event_commands";
import { EventCommandsImpl } from "./event_commands_impl";

export const eventsBackSupport: CommandBackSupport<EventCommands> = {
    [MobileWebkitEngine.Legacy]: (mobileWebKit) => new EventCommandsImpl(mobileWebKit),
    [MobileWebkitEngine.V200]: (mobileWebKit) => new EventCommandsImpl(mobileWebKit)
};
