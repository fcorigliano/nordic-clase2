export interface DownloadImageOptions {
    path: string;
    authenticated?: boolean; // verificar si en ios se necesita
    method?: string;
    base_url?: string; // verificar si en ios se necesita
    params?: { [key: string]: string };
    headers?: { [key: string]: string }; // verificar si en ios se necesita
}
