export interface ShareImageOptions {
    path: string;
    authenticated?: boolean; // Verificar si se necesita en ios
    method?: string;
    base_url?: string; // Verificar si se necesita en ios
    params?: { [key: string]: string };
    headers?: { [key: string]: string }; // Verificar si se necesita en ios
}
