import { MobileWebkitEngine } from "../../mobile-webkit-engine";
import { CommandBackSupport } from "../utils/command-backsupport";
import { ActionCommands } from "..";
import { ActionCommandsLegacy } from "./backsupport/actions_commands_legacy";
import { ActionsCommandsImpl } from "./actions_commands_impl";

export const actionsbackSupport: CommandBackSupport<ActionCommands> = {
    [MobileWebkitEngine.Legacy]: (mobileWebKit) => new ActionCommandsLegacy(mobileWebKit),
    [MobileWebkitEngine.V200]: (mobileWebKit) => new ActionsCommandsImpl(mobileWebKit)
}
