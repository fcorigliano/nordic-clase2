import { MobileWebkitEngine } from "../../mobile-webkit-engine";
import { CommandBackSupport } from "../utils/command-backsupport";
import { SettingsCommandsLegacy } from "./backsupport/settings_commands_legacy";
import { SettingsCommands } from "./settings_commands";
import { SettingsCommandsImpl } from "./settings_commands_impl";

export const settingsBackSupport: CommandBackSupport<SettingsCommands> = {
    [MobileWebkitEngine.Legacy]: (mobileWebKit) => new SettingsCommandsLegacy(mobileWebKit),
    [MobileWebkitEngine.V200]: (mobileWebKit) => new SettingsCommandsImpl(mobileWebKit),
};
