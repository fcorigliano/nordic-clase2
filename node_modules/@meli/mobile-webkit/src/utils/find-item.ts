export function findItem<T>(data: T[], criteria: (item: T) => boolean): T | undefined {
    for (let item of data) {
        if (criteria(item)) {
            return item;
        }

    }
    return undefined;
}
