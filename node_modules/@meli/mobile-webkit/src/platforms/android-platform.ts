import { Platform } from "./platform";

declare let WebviewSDKCommunicationChannel: any;

export class AndroidPlatform implements Platform {

    isAvailable(): boolean {
        return (window as any).WebviewSDKCommunicationChannel != null
    }

    sendMessage(message: string) {
        WebviewSDKCommunicationChannel.executeNative(message);
    }
}