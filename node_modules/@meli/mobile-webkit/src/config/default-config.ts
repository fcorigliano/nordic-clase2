import { WebkitConfig } from "./webkit-config";

export const DefaultConfig: WebkitConfig = {
    connect: {
        timeout: 1000,
        retries_on_timeout: 3,
        retry_time_on_timeout: 0
    },
    request: {
        timeout: 0,
        retries_on_timeout: 0,
        retry_time_on_timeout: 0
    },
    multipleRequest: {
        timeout: 0,
        retries_on_timeout: 0,
        retry_time_on_timeout: 0
    }
}