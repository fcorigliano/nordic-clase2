module.exports = function fetchTraceabilityConsent(req) {
    if (req.testCase === 'traceability-true') {
        return true;
    }
    if (req.testCase === 'traceability-false') {
        return false;
    }
    if (req.testCase === 'middleware-failed') {
        return false;
    }
}