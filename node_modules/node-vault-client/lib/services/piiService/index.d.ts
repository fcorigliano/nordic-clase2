import type { PIIServiceParams, VaultResponseData, PIIServiceConfig } from './types';
/**
 * Service to get PII info from Vault API v2.
 * Info on API can be found [here](https://sites.google.com/mercadolibre.com/kyc/data-vault)
 */
export default class PIIService {
    #private;
    constructor(config: PIIServiceConfig);
    getData<T>(params: PIIServiceParams): Promise<VaultResponseData<T>>;
    private makeRequest;
}
