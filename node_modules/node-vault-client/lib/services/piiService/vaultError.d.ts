import type { VaultErrorData } from './types';
declare class VaultError extends Error {
    extensions: {
        code: string;
    };
    isVaultError: boolean;
    constructor(errorData: VaultErrorData);
}
export default VaultError;
