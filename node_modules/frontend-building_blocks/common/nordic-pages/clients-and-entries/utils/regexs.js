const whiteSpaceRegex = /\s/g;

const expectedHydrateLineRegex = /export(const|var)hydrate=/;

const hydrateExportedAsTrueRegex = new RegExp(`${expectedHydrateLineRegex.source}true`);
const hydrateExportedAsFalseRegex = new RegExp(`${expectedHydrateLineRegex.source}false`);

module.exports = {
  expectedHydrateLineRegex,
  hydrateExportedAsTrueRegex,
  hydrateExportedAsFalseRegex,
  whiteSpaceRegex,
};
