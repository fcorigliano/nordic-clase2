const config = require('frontend-config');
const telemetry = require('./telemetry');

const isConformanceEnabled = () => {
  const enabled = config.get('conformance') !== false;

  telemetry.trackEvent('Conformance', enabled ? 'Enabled' : 'Disabled');

  return enabled;
};

module.exports = {
  isConformanceEnabled,
};
