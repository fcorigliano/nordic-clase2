const { existsSync } = require('fs');
const { NORDIC_PAGES_FOLDER } = require('./constants');

const nordicPagesEnabled = NORDIC_PAGES_FOLDER && existsSync(NORDIC_PAGES_FOLDER);

module.exports = {
  nordicPagesEnabled,
};
