const { resolve } = require('path');
const logger = require('frontend-logger');

const log = logger('frontend-building_blocks');

const NORDIC_PAGES_FOLDER = resolve('./app/nordic-pages');

const CLIENT_PATH = resolve('./.nordic/client');

const POLYFILLS_FILE = resolve('./app/nordic-pages/polyfills.js');

const POLYFILLS_ENTRY_NAME = `${process.env.npm_package_name}-polyfills`;

module.exports = {
  log,
  NORDIC_PAGES_FOLDER,
  CLIENT_PATH,
  POLYFILLS_FILE,
  POLYFILLS_ENTRY_NAME,
};
