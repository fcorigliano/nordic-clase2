const env = require('frontend-env');

const defaultConfig = {
  host: 'datadog',
  prefix: `application.${env.APPLICATION || env.npm_package_name}.`,
  maxBufferSize: env.FURY && env.PRODUCTION ? 8192 : 0, // This is the maximum value enabled by the datadog Agent
  mock: !env.FURY || env.IS_CI,
  globalTags: {
    platform: env.PLATFORM,
    application: env.APPLICATION,
    scope: env.SCOPE,
  },
};

module.exports = defaultConfig;
