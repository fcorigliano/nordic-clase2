const React = require('react');

const { observe } = require('react-performance-observer');

class PerformanceObserver extends React.Component {
  componentDidMount() {
    observe((measurements) => {
      console.log('React Performance Observer', measurements);
    });
  }

  render() {
    return null;
  }
}

module.exports = PerformanceObserver;

