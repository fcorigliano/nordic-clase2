const path = require('path');

module.exports = {
  mode: 'development',
  entry: './mocks/hotReload/foo.js',
  output: {
    filename: '[name].js',
    publicPath: 'https://http2.mlstatic.com/resources/frontend/statics/test',
    path: path.resolve(__dirname, 'build'),
  },
  module: {
    rules: [
      {
        test: /\.(svg|html)$/,
        type: 'asset/resource',
      },
    ],
  },
};