/**
 * Module dependencies
 */
const express = require('express');
const { PRODUCTION } = require('frontend-env');

/**
 * Expose static middleware
 */
module.exports = (config) => {
  if (PRODUCTION) {
    return (req, res, next) => next();
  }
  return express.static(config.server.static);
};
