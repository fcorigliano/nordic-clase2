/**
 * Module dependencies
 */
const express = require('express');
const Ragnar = require('./lib/Ragnar');

/**
 * Expose Ragnar factory
 */
exports = module.exports = (config) => new Ragnar(config); // eslint-disable-line no-multi-assign

/**
 * Router
 */
exports.router = express.Router;
