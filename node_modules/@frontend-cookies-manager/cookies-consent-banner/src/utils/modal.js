const embedModeEvents = {
  SAVE_COOKIES_PREFERENCES: 'saveCookiesPreference',
};

const embedModeStatus = {
  SUCCESS: 'SUCCESS',
  FAILURE: 'FAILURE',
};

const getModalConfig = (namespace) => ({
  id: `js-modal-${namespace}`,
  className: `${namespace}__modal`,
  overlayClassName: `${namespace}__modal-overlay`,
  wrapperClassName: `${namespace}__modal-wrapper`,
});

module.exports = {
  embedModeEvents,
  embedModeStatus,
  getModalConfig,
};
