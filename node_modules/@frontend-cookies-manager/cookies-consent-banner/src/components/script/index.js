const withSideEffect = require('react-side-effect');

const Component = () => null;

// return propsList
const identity = (propsList) => propsList;

// Do nothing
const handleStateChangeOnClient = () => {};

const ComponentWithSideEffect = withSideEffect(identity, handleStateChangeOnClient, identity)(Component);

module.exports = ComponentWithSideEffect;
