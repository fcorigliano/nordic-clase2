const {
  createConfig,
  entryPoint,
  customConfig,
} = require('frontend-building_blocks');
const uiPreset = require('frontend-building_blocks/presets/ui');

const config = createConfig([
  entryPoint({
    components: './packages/cookies-consent-banner/src/components/index.js',
  }),
  uiPreset({
    buildPath: './packages/cookies-consent-banner/lib',
  }),
  customConfig({
    target: 'node',
    node: {
      __dirname: false,
    },
  }),
]);

/**
 * Expose config
 */
module.exports = config;
