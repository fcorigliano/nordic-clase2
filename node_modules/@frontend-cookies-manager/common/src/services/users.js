const getUserId = req => (
  req && (
    (req.userSession && req.userSession.operator_id)
      || (
        req.user && (
          req.user.rootId || req.user.id
        )
      )
  )
) || null;

module.exports = {
  getUserId,
};
