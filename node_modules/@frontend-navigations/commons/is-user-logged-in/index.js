/**
 * Returns true if the props passed contain an object user
 * @param {object} props - usually the request or props passed on from frontend-layout
 * @returns {bool}
*/
const isUserLoggedIn = (props) => {
  const { user } = props;

  return !!user;
};

module.exports = isUserLoggedIn;

