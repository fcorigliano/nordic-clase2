import type { Request } from 'express';
export declare type ReauthUrlParams = {
    req: Request;
    transactionId: string;
    redirectUrl?: string;
    deeplink?: string;
};
export declare const reauthRestrictedURL: () => string;
export declare const createURLFromPath: (req: Request, path: string) => string;
/**
 * Get URL to redirect to when reauth fails
 */
export declare const reauthErrorURL: (req: Request) => string;
/**
 * Returns the URL to redirect a to reauthenticate
 */
export declare const reauthenticationURL: (params: ReauthUrlParams) => string;
