import type { Request } from 'express';
import type { ReauthServiceParams } from '../data/models';
declare type ReauthServiceResponse = {
    requireReauth: false;
} | {
    requireReauth: true;
    reauthUrl: string;
    transactionId: string;
};
/**
 * @param req Express Request
 * @param params Params required by Reauth Service
 * @param calledInMiddleware Flag indicating if Service is being called from reauth middleware or standalone
 */
export declare const internalCheckReauthService: (req: Request, params: ReauthServiceParams, calledInMiddleware?: boolean) => Promise<ReauthServiceResponse>;
export declare const checkReauthService: (req: Request, params: ReauthServiceParams) => Promise<ReauthServiceResponse>;
export {};
