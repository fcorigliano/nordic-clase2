import type { Request, Response } from 'express';
import { reauthDefaultConfig } from './data/constants';
export declare const setReauthCookie: (value: string, req: Request, res: Response, cookieExtraOptions?: {}) => void;
export declare const getUserIP: (req: Request) => string;
declare type JsonResponse = Record<string, unknown> & {
    message: string;
};
export declare const getConfigByOperationId: (operationId: string) => typeof reauthDefaultConfig;
/**
 * Generate a JSON object for xhr responses
 */
export declare const getJsonResponse: (httpCode: number, data?: Record<string, unknown>) => JsonResponse;
export {};
