const React = require('react');
const PropTypes = require('prop-types');

const Bounds = ({ children }) => (
  <div className="fp-nav-bounds">
    { children }
  </div>
);

Bounds.propTypes = {
  children: PropTypes.element.isRequired,
};

module.exports = Bounds;
