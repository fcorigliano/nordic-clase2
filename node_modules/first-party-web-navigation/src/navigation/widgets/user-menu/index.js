/* eslint-disable import/no-extraneous-dependencies */
const React = require('react');
const Script = require('frontend-script');
const script = require('../../client/user-menu');

const UserMenuWidget = () => (
  <Script>
    { `var userMenu = ${script}()` }
  </Script>
);

module.exports = UserMenuWidget;
