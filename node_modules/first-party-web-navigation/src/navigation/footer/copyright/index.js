const React = require('react');
const PropTypes = require('prop-types');

const namespace = 'fp-nav-copyright';

const Copyright = ({ text }) => (
  <div className={namespace}>
    { text }
  </div>
);

Copyright.propTypes = {
  text: PropTypes.string.isRequired,
};

module.exports = Copyright;
