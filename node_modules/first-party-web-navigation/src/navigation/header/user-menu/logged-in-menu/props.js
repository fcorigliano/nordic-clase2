const userMenuProps = (props) => {
  const {
    links,
    myAccountText,
    user,
    navigationPIIData,
  } = props;

  return {
    userName: navigationPIIData.preferredName || user.nickname,
    links,
    myAccountText,
  };
};

module.exports = userMenuProps;
