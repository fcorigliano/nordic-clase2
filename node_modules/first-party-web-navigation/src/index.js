const navigation = require('./navigation');
const prepareToRender = require('./prepare-to-render');

navigation.prepareToRender = prepareToRender;
navigation.id = 'First-party';

module.exports = navigation;
