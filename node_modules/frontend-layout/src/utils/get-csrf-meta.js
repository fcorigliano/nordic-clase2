/**
 * Create HTML Meta Tag to store current csrf token
 * @param {String} token
 */
module.exports = function getCSRFMeta(token) {
  return token
    ? `<meta name="csrf-token" content="${token}" />`
    : '';
};
