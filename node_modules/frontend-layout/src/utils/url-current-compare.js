module.exports = function isSameAsCurrentURL(currentURL, url) {
  const urlModified = url.replace('https://home', 'https://www').replace(/\/$/, '');
  const currentURLModified = currentURL.replace(/\/$/, '');
  return currentURLModified === urlModified;
};
