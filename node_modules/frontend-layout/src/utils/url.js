function getFullUrlFromReq(req) {
  const proto = (req.headers && req.headers.ml_ssl === '1')
    || (req.connection && req.connection.encrypted) ? 'https' : 'http';
  return `${proto}://${req.get('host')}${req.originalUrl}`;
}

module.exports = {
  getFullUrlFromReq,
};

