const BO = require('./platforms/bo');
const ML = require('./platforms/ml');
const MP = require('./platforms/mp');
const TC = require('./platforms/tc');
const TI = require('./platforms/ti');
const MT = require('./platforms/mt');
const TL = require('./platforms/tl');
const MS = require('./platforms/ms');
const PI = require('./platforms/pi');

module.exports = {
  BO, ML, MP, MT, TC, TI, TL, MS, PI,
};
