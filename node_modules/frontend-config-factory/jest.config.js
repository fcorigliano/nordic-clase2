module.exports = {
  verbose: true,
  collectCoverage: true,
  coverageReporters: ['text', 'html', 'lcov', 'text-summary'],
  roots: ['<rootDir>/src/'],
  coverageDirectory: '<rootDir>/coverage',
  testMatch: ['**/?(*.)(spec).js?(x)'],
};
