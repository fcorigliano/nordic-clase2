/**
 * Module dependencies
 */

/**
 * Exports module with name and version of OS and browser
 * @returns Object
 */
module.exports = (agent) => ({
  osName: agent.os.name?.toLowerCase(),
  osVersion: agent.os.version,
});
