const {
  APPLICATION,
  FURY,
} = require('frontend-env');

const applications = {
  'dynamic-content-libs/example': true,
  'frontend-cookies-manager/example': true,
};

const exampleApplication = !FURY && applications[APPLICATION];

const isExampleApplication = () => exampleApplication;

module.exports = {
  isExampleApplication,
};
