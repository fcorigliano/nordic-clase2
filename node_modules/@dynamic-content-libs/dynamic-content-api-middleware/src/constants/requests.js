const env = require('frontend-env');

const isProduction = env.FURY && env.PRODUCTION;

module.exports = {
  dynamicContent: {
    restClientConfig: {
      timeout: isProduction ? 200 : 20000,
      retry: {
        maxRetries: 0,
        delay: 0,
      },
    },
    url: '/dynamic-content/api/dynamic-content',
    scopeHeader: 'x-dynamic-content-scope',
    localScopeHeaderValue: 'local',
    labCookie: 'meliLab',
    localBaseUrl: 'https://dev.mercadolibre.com.ar:8444',
  },
};
