describe('Root index', () => {
  beforeEach(() => jest.resetModules());

  it('should return middleware from src', () => {
    const middlewareMock = jest.fn();
    jest.doMock('./src/index.js', () => middlewareMock);

    const indexModule = require('./index');

    expect(indexModule).toBe(middlewareMock);
  });
});
