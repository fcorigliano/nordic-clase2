import { AppConfigService } from '@auth/config-service';
export declare const configService: AppConfigService<{
    timeouts: {
        rotok: number;
    };
    forward_headers: {
        'user-agent': string;
    };
    forward_cookies: string[];
    forward_req_as_body: {
        operator_id: string;
        root_user_id: string;
    };
}>;
