import type { Request } from 'express';
import type { Platform } from 'frontend-platform_detection';
export declare type RotokResponse = undefined | {
    /**
     * Rotok token to set as the nsa_rotok cookie
     */
    token: string;
    /**
     * Remaining time in seconds for the cookie to expire
     *
     * @example 1800
     */
    max_age: number;
};
export declare const checkSessionAnomalies: (req: Request, platform: Platform) => Promise<RotokResponse>;
