export declare const PACKAGE_VERSION: string;
export declare const CLIENT_UA: string;
export declare const BANNED_COOKIES: string[];
export declare const ROTOK_COOKIE_NAME = "nsa_rotok";
export declare const TEST_COOKIE_NAME = "nsa_rotok_dev";
declare global {
    namespace Express {
        interface Request {
            /**
             * Array of promises that should be awaited
             * before sending the response to the client.
             */
            pendingPromises: Promise<any>[];
            cookies: Record<string, string>;
        }
    }
}
