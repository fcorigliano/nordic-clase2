import type { AnyObject } from './utils';
export interface AppConfigServiceParams<T extends AnyObject = AnyObject> {
    app: string;
    extraParams?: AnyObject;
    defaultConfig?: T;
}
export declare class AppConfigService<T extends AnyObject = AnyObject> {
    #private;
    constructor({ app, extraParams, defaultConfig }: AppConfigServiceParams<T>);
    getLatestConfig(): T;
}
