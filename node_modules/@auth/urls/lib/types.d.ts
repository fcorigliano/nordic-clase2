import type { PlatformId } from 'frontend-platform_detection';
export interface LoginParams {
    /**
     * URL to redirect to once user has logged in.
     * Defaults to current request URL.
     */
    go?: string;
    /**
     * Platform ID to personalize login's visual.
     * Defaults to current platform.
     */
    platform_id?: PlatformId;
    /**
     * ID to tell the login the context of it.
     * More info on the [documentation](https://github.com/mercadolibre/fury_auth-frontend-library/tree/master/modules/urls#customize-urls-parameters).
     */
    loginType?: string;
}
export interface RegistrationParams {
    /**
     * URL to redirect to once user has finished registering.
     * Defaults to current request URL.
     */
    confirmation_url?: string;
}
export interface LogoutParams {
    /**
     * URL to redirect to once user has logged out.
     */
    go?: string;
}
export interface AccessLiteParams {
    /**
     * URL to redirect to once user got a session.
     */
    callback_url?: string;
    /**
     * Session id. of device profile got by running profiling widget.
     */
    dps?: string;
}
export declare type UrlParams = LoginParams | RegistrationParams | LogoutParams | AccessLiteParams;
declare global {
    namespace Express {
        interface Request {
            /**
             * Parameters to customize login URL when [@auth/urls](https://github.com/mercadolibre/fury_auth-frontend-library/tree/master/modules/urls) module is used.
             * Please refer to its documentation for more information.
             */
            loginParams?: LoginParams;
            /**
             * Parameters to customize registration URL when [@auth/urls](https://github.com/mercadolibre/fury_auth-frontend-library/tree/master/modules/urls) module is used.
             * Please refer to its documentation for more information.
             */
            registrationParams?: RegistrationParams;
            /**
             * Parameters to customize logout URL when [@auth/urls](https://github.com/mercadolibre/fury_auth-frontend-library/tree/master/modules/urls) module is used.
             * Please refer to its documentation for more information.
             */
            logoutParams?: LogoutParams;
            /**
             * Parameters to customize access lite URL when [@auth/urls](https://github.com/mercadolibre/fury_auth-frontend-library/tree/master/modules/urls) module is used.
             * Please refer to its documentation for more information.
             */
            accessLiteParams?: AccessLiteParams;
        }
    }
}
