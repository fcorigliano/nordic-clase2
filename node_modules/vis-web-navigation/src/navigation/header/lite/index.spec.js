const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const HeaderLite = require('.');

const baseProps = {
  type: 'full',
  siteId: 'MLV',
  logo: {
    url: 'https://www.tucarro.com.ve',
    label: 'Classifieds',
  },
  melilab: {},
  userMenu: {
    isUserLoggedIn: false,
  },
  searchbox: {
    isDesktop: true,
  },
  navMenuSwitchId: 'navMenuSwitchId',
};

describe('Classifieds Navigation', () => {
  describe('Header Lite', () => {
    it('returns the header in lite mode, web mobile', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <HeaderLite
          {...baseProps}
          lite={{
            userLinks: {},
            isDesktop: false,
          }}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });

    it('returns the header in lite mode, desktop', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <HeaderLite
          {...baseProps}
          lite={{
            userLinks: {
              links: [],
            },
            isDesktop: true,
          }}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
