const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const LoggedInUserMenu = require('.');

const baseProps = {
  userLinksSections: {
    sectionsLinks: [
      [
        {
          link: {
            href: 'https://www.tucarro.com.ve',
          },
          name: 'Inicio',
          icon: 'nav-home-icon',
          selected: false,
          id: 'home',
        },
      ],
      [
        {
          link: {
            href: 'https://www.tucarro.com.ve/ofertas',
          },
          name: 'Ofertas',
          icon: 'nav-deals-icon',
          selected: false,
          id: 'deals',
        },
      ],
    ],
  },
  welcome: {
    avatar: {
      alt: 'Test',
    },
    nickname: 'test',
    email: 'test@test.com',
  },
};

describe('Classifieds Navigation', () => {
  describe('Logged In User Menu Mobile Component', () => {
    it('matches the default snapshot for LoggedInUserMenu component', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <LoggedInUserMenu
          {...baseProps}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
