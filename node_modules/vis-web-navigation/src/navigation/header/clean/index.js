const React = require('react');
const PropTypes = require('prop-types');
const Header = require('../components/base-header');
const UserMenu = require('../components/user-menu');

const HeaderClean = (props) => {
  const { userMenu, navMenuSwitchId } = props;

  return (
    <Header
      {...props}
    >
      <div className="nav-header-menu-wrapper">
        <UserMenu {...userMenu} navMenuSwitchId={navMenuSwitchId} />
      </div>
    </Header>
  );
};

HeaderClean.propTypes = {
  // eslint-disable-next-line react/forbid-prop-types
  userMenu: PropTypes.object.isRequired,
};

module.exports = HeaderClean;
