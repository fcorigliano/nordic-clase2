const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const FooterDesktop = require('./desktop');

describe('Classifieds Navigation', () => {
  describe('Footer Lite Desktop', () => {
    it('matches the default snapshot for Footer Lite Desktop in desktop', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <FooterDesktop
          footerNavigation={{
            links: [{
              href: 'https://www.tucarro.com.ve/jobs',
              name: 'Trabaja con nosotros',
              id: 'jobs',
            }],
          }}
          copyright={{
            since: '1999',
            companyName: 'Mercado Libre',
            currentYear: 2019,
          }}
          userMenu={{
            links: [{
              href: 'https://www.tucarro.com.ve/registration',
              name: 'Creá tu cuenta',
              id: 'registration',
            }],
            isUserLoggedIn: false,
          }}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
