const FULL = 'full';
const LITE = 'lite';
const HIDDEN = 'hidden';
const INTERNAL = 'internal';
const CLEAN = 'clean';

const types = {
  full: FULL,
  lite: LITE,
  hidden: HIDDEN,
  internal: INTERNAL,
  clean: CLEAN,
};

module.exports = {
  FULL,
  LITE,
  HIDDEN,
  INTERNAL,
  CLEAN,
  types,
};
