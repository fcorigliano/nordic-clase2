const prepareToRender = require('.');

const baseRequest = {
  platform: {
    id: 'TC',
    siteId: 'MLV',
  },
  device: {
    mobile: false,
  },
  cookies: {},
  headers: {},
  query: {},
};

const baseResponse = {};
const baseParams = {
  type: 'full',
  urls: {
    mainDomain: {
      link: 'tucarro.com',
    },
  },
  configSchema: {
    headerMenu: [],
  },
  features: {
    loansMessages: true,
  },
};

describe('Classifieds - Prepare to render', () => {
  it('returns the different results for the required services', (done) => {
    prepareToRender(baseParams, baseRequest, baseResponse).then((results) => {
      expect(results.userInfo).toBeDefined();
      done();
    });
  });
});
