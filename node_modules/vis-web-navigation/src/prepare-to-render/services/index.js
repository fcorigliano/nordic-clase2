const {
  execute: cookieDisclaimer,
} = require('@frontend-navigations/commons/prepare-to-render/services/cookie-disclaimer');
const userMenu = require('./user-menu');
const pii = require('./pii');
const navigationFeatures = require('./navigation-features');
const req = require('./req');
const urls = require('./urls');

const services = [
  { key: 'cookieDisclaimer', execute: cookieDisclaimer },
  { key: 'userInfo', execute: userMenu },
  { key: 'navigationPIIData', execute: pii },
  { key: 'navigationFeatures', execute: navigationFeatures },
  { key: 'req', execute: req },
  { key: 'navigationUrls', execute: urls },
];

module.exports = services;
