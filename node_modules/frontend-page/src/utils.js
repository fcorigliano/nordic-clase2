const logger = require('frontend-logger');
const { possibleAdaptiveOptions } = require('./constants');

const log = logger('frontend-page');

const isAdaptiveEnabled = (adaptive, adaptiveFor) => possibleAdaptiveOptions.both === adaptive || possibleAdaptiveOptions[adaptiveFor] === adaptive;

module.exports = {
  isAdaptiveEnabled,
  log,
};
