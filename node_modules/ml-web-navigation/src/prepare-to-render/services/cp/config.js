const restClientTimeout = isProduction => (isProduction ? 200 : 1000);
const cookieMaxAge = 60 * 60 * 24 * 30 * 1000; // 2592000000 1 month max age in miliseconds

module.exports = {
  restClientTimeout,
  cookieMaxAge,
};
