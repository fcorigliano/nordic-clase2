const React = require('react');
const serialize = require('serialize-javascript');
const Script = require('frontend-script');

const PreloadedState = props => (
  <Script on="now">{`window.__NAVIGATION_PRELOADED_STATE__ = ${serialize(props, { isJSON: true })};`}</Script>
);

module.exports = PreloadedState;
