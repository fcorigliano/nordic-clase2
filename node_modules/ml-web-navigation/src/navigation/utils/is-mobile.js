/**
 * Returns true if the props passed contain an object device and device.mobile is true
 * @param {object} props - usually the request or props passed on from frontend-layout
 * @returns {bool}
*/
const isMobile = (props) => {
  const { device } = props;

  return device.mobile || false;
};

module.exports = isMobile;
