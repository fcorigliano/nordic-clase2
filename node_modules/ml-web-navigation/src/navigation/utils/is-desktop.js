/**
 * Returns true if the props passed contain an object device and device.desktop is true
 * @param {object} props - usually the request or props passed on from frontend-layout
 * @returns {bool}
*/
const isDesktop = (props) => {
  const { device } = props;

  return device.desktop || false;
};

module.exports = isDesktop;
