/**
 * Returns true if the props passed contain an object device and device.webView is true
 * @param {object} props - usually the request or props passed on from frontend-layout
 * @returns {bool}
*/
const isWebView = (props) => {
  const { device } = props;
  return device.webView || false;
};

module.exports = isWebView;
