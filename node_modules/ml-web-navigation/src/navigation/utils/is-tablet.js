/**
 * Returns true if the props passed contain an object device and device.tablet is true
 * @param {object} props - usually the request or props passed on from frontend-layout
 * @returns {bool}
*/
const isTablet = (props) => {
  const { device } = props;
  return device.tablet || false;
};

module.exports = isTablet;
