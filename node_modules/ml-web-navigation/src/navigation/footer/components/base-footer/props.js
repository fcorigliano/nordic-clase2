const { getValue } = require('ml-navigation-config');

const baseFooterProps = (props) => {
  const { req, platform } = props;

  const extraCompanyName = getValue({
    key: 'extraCompanyName',
    req,
    platform,
  });

  return {
    extraCompanyName,
  };
};

module.exports = baseFooterProps;
