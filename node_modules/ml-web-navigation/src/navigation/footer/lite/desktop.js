/* eslint-disable react/forbid-prop-types */
const React = require('react');
const PropTypes = require('prop-types');
const FooterNavigation = require('../components/footer-navigation');
const BaseFooter = require('../components/base-footer');
const DownloadAppBanner = require('../components/download-app-banner');

const FooterDesktop = (props) => {
  const { footerNavigation, downloadAppBanner, ...baseFooterProps } = props;

  return (
    <BaseFooter
      {...baseFooterProps}
      footerExhibitor={
        <DownloadAppBanner {...downloadAppBanner} />
      }
    >
      <FooterNavigation {...footerNavigation} />
    </BaseFooter>
  );
};

FooterDesktop.propTypes = {
  footerNavigation: PropTypes.object.isRequired,
  downloadAppBanner: PropTypes.object.isRequired,
};

module.exports = FooterDesktop;
