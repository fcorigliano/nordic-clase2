const React = require('react');
const Header = require('../components/base-header');
const { types } = require('../../../layout-type-detection');

const HeaderLite = (props) => {
  const liteType = types.LITE;

  return (
    <Header
      type={liteType}
      {...props}
    />
  );
};

module.exports = HeaderLite;
