/* eslint-disable security/detect-non-literal-fs-filename */
const CP = require('.');
const cpProps = require('./props');

const id = 'cpLocation';

const cpEnhancement = (url, props) => {
  const newUrl = { ...url,
    Component: CP,
    ...cpProps(props) };

  return newUrl;
};

module.exports = {
  id,
  enhance: cpEnhancement,
};
