const userLinksSectionsProps = require('../user-links-sections/props');
const welcomeProps = require('./welcome/props');
const modelessBoxProps = require('../../../../../widgets/modeless-box/props');

const loggedInUserMenuProps = (props, options = {}) => ({
  userLinksSections: userLinksSectionsProps(props, 'logged'),
  welcome: welcomeProps(props, options),
  modelessBox: modelessBoxProps(props),
});

module.exports = loggedInUserMenuProps;

