const DECIMAL_SEPARATOR_COMMA = ',';
const DECIMAL_SEPARATOR_DOT = '.';

const DECIMAL_SEPARATOR_BY_CENTS_TYPE = {
  comma: DECIMAL_SEPARATOR_COMMA,
  dot: DECIMAL_SEPARATOR_DOT,
};

module.exports = {
  DECIMAL_SEPARATOR_BY_CENTS_TYPE,
  DECIMAL_SEPARATOR_COMMA,
};
