const React = require('react');
const PropTypes = require('prop-types');
const UserLinksSections = require('../user-links-sections');
const Welcome = require('./welcome');

const MobileLoggedInUserMenu = ({
  userLinksSections,
  welcome,
  linkTags,
  modelessBox,
}) => (
  <>
    <Welcome {...welcome} />
    <UserLinksSections
      {...userLinksSections}
      linkTags={linkTags}
      modelessBox={modelessBox}
    />
  </>
);

MobileLoggedInUserMenu.propTypes = {
  userLinksSections: PropTypes.object.isRequired,
  welcome: PropTypes.object.isRequired,
  modelessBox: PropTypes.object,
};

MobileLoggedInUserMenu.defaultProps = {
  modelessBox: null,
};

module.exports = MobileLoggedInUserMenu;
