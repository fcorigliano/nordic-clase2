const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const Coins = require('./coins');

describe('Mercado Libre Navigation', () => {
  describe('Coins Component', () => {
    it('matches the default snapshot for MeliCoins component', () => {
      const renderer = new ShallowRenderer();
      const kyc = {
        label: 'string',
        target: 'string',
      };
      const array = [
        {
          id: 'string',
          label: 'string',
          icon: 'string',
          target: 'string',
          currency: 'string',
          symbol: 'string',
          balance: {
            units: 'long',
            cents: 'long',
          },
          exchange: {
            label: 'string',
            accessibility_label: 'string',
          },
          exchange_rate: 'double',
          variation: {
            color: 'string',
            label: 'string',
            accessibility_label: 'string',
          },
        },
      ];
      const index = array.lenght;
      const component = (
        <Coins
          kyc={kyc}
          index={index}
          array={array}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
