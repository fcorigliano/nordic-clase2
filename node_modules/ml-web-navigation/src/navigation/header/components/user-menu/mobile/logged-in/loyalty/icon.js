const React = require('react');
const PropTypes = require('prop-types');

const LoyaltyIcon = (props) => {
  const { namespace, className } = props;

  return (
    <svg className={`${namespace}-link-icon`} xmlns="http://www.w3.org/2000/svg" width="7" height="10" viewBox="0 0 7 10">
      <path
        className={className}
        fill="none"
        fillRule="evenodd"
        strokeWidth="1.5"
        d="M1.017 9.032l4.015-4.024L1.017.983"
      />
    </svg>
  );
};

LoyaltyIcon.propTypes = {
  namespace: PropTypes.string.isRequired,
  className: PropTypes.string.isRequired,
};

module.exports = LoyaltyIcon;
