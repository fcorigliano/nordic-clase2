const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const Melicoins = require('.');

const baseProps = {
  mercadoPago: {
    title: {
      label: 'string',
      icon: 'string',
    },
    coins: [
      {
        id: 'string',
        label: 'string',
        icon: 'string',
        target: 'string',
        currency: 'string',
        symbol: 'string',
        balance: {
          units: 'long',
          cents: 'long',
        },
        exchange: {
          label: 'string',
          accessibility_label: 'string',
        },
        exchange_rate: 'double',
        variation: {
          color: 'string',
          label: 'string',
          accessibility_label: 'string',
        },
      },
    ],
    kyc: {
      label: 'string',
      target: 'string',
    },
  },
};

describe('Mercado Libre Navigation', () => {
  describe('Mercado Pago Mobile Component', () => {
    it('matches the default snapshot for Melicoins component', () => {
      const renderer = new ShallowRenderer();
      const component = <Melicoins {...baseProps} />;
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
