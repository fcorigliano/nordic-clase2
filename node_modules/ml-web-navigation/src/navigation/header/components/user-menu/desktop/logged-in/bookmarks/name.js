const React = require('react');
const PropTypes = require('prop-types');

const BookmarksName = ({ name }) => <span className="bookmarks-text">{name}</span>;

BookmarksName.propTypes = {
  name: PropTypes.string.isRequired,
};

module.exports = BookmarksName;
