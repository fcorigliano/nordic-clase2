const cpEnhancement = require('../cp/enhancement');
const enhancements = require('./enhancements');

describe('Mercado Libre Navigation', () => {
  describe('Nav Menu Enhancements', () => {
    it('has the expected enhancements', () => {
      expect(enhancements[cpEnhancement.id]).toBeDefined();
      expect(enhancements[cpEnhancement.id]).toEqual(cpEnhancement);
    });
  });
});
