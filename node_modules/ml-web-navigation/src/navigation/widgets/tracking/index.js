const React = require('react');
const Script = require('frontend-script');
const readClientFile = require('../../utils/read-client-file');

const clientFileContent = readClientFile('navigation-tracking.js');

const Tracking = () => (
  <Script on="now">
    {clientFileContent}
  </Script>
);

module.exports = Tracking;
