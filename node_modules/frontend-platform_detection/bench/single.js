/**
 * Module dependencies
 */
const platform = require('../src/platform');

console.time('single-platform');

for (let i = 1; i < 1E5; i += 1) {
  platform('http://listado.mercadolibre.com.mx');
}

console.timeEnd('single-platform');
