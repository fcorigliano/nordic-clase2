/**
 * Grabs the 'cookie-guest-cart' query param from request
 * @param {express.request}
 */
const { COOKIES } = require('../config');

module.exports = (req) => {
  const { cookies = {} } = req;
  return (cookies[COOKIES.GUEST_CART_ID]);
};
