const HEADER_KEY = 'X-Mshops-Section';

/**
 * Grabs the section from the request if explicited
 * @param {express.request} req
 */
module.exports = (req) => {
  const hasSectionHeader = (req.headers && req.header(HEADER_KEY));
  return hasSectionHeader ? req.header(HEADER_KEY) : 'home';
};
