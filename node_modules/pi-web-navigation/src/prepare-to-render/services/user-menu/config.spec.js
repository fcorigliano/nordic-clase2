const config = require('./config');

describe('Portal Inmobiliario - Prepare to Render', () => {
  describe('User Menu Service Config', () => {
    it('returns a specific timeout depending on the environment', () => {
      expect(config.restClientTimeout(true)).toEqual(200);
      expect(config.restClientTimeout(false)).toEqual(1000);
    });
  });
});
