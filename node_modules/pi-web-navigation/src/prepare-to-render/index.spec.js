const prepareToRender = require('./index');

const baseRequest = {
  platform: {
    id: 'ML',
    siteId: 'MLA',
  },
  device: {
    mobile: false,
  },
  cookies: {},
  headers: {},
  query: {},
};

const baseResponse = {};
const baseParams = {
  type: 'plus',
  urls: {
    mainDomain: {
      link: 'portalinmobiliario.com',
    },
  },
  configSchema: {
    headerMenu: [],
  },
  features: {
    loansMessages: true,
  },
};

describe('Portal Inmobiliario - Prepare to render', () => {
  it('returns the different results for the required services', (done) => {
    prepareToRender(baseParams, baseRequest, baseResponse).then((results) => {
      expect(results.userInfo).toBeDefined();
      done();
    });
  });
});
