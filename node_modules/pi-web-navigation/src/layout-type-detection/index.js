const types = require('./types');

const isPlus = type => type === types.PLUS;
const isHidden = type => type === types.HIDDEN;
const isLite = type => type === types.LITE;
const isInternal = type => type === types.INTERNAL;

module.exports = {
  isPlus,
  isHidden,
  isLite,
  isInternal,
  types,
};
