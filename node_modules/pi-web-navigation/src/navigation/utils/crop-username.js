const cropUsername = (username) => {
  const nameMaxLength = 11;
  return username.length > nameMaxLength
    ? `${username.slice(0, nameMaxLength - 3)}...`
    : username;
};

module.exports = cropUsername;
