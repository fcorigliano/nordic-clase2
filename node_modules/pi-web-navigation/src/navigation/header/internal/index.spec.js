const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const HeaderInternal = require('.');

const baseProps = {
  type: 'plus',
  siteId: 'MLA',
  logo: {
    url: 'https://www.mercadolibre.com.ar',
    label: 'Mercado Libre',
  },
  melilab: {},
  userMenu: {
    isUserLoggedIn: false,
  },
  navMenuSwitchId: 'navMenuSwitchId',
};

describe('Portal Inmobiliario Navigation', () => {
  describe('Header Internal', () => {
    it('returns the header in internal mode', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <HeaderInternal
          {...baseProps}
          type="internal"
          siteId="MLA"
          internal={{
            title: 'Internal',
            btnHref: '/',
          }}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });

    it('returns the header in internal mode with no title and back href', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <HeaderInternal
          {...baseProps}
          type="internal"
          siteId="MLA"
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
