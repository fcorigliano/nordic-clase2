const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const FooterPlus = require('./index');

describe('Portal Inmobiliario Navigation', () => {
  describe('Footer Plus', () => {
    it('matches the default snapshot for Footer in mobile', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <FooterPlus
          footerNavigation={{
            links: [{
              href: 'https://www.portalinmobiliario.com.ar/jobs',
              name: 'Trabaja con nosotros',
              id: 'jobs',
            }],
          }}
          footerLinks={{
            links: [{
              href: 'https://www.portalinmobiliario.com.ar/jobs',
              name: 'Trabaja con nosotros',
              id: 'jobs',
            }],
          }}
          userMenu={{
            links: [{
              href: 'https://www.portalinmobiliario.com.ar/registration',
              name: 'Creá tu cuenta',
              id: 'registration',
            }],
            isUserLoggedIn: false,
          }}
        />
      );

      const renderedComponent = renderer.render(component);

      expect(renderedComponent).toMatchSnapshot();
      expect(renderedComponent.type.name).toEqual('FooterMobile');
    });

    it('matches the default snapshot for Footer in desktop', () => {
      const renderer = new ShallowRenderer();
      const component = (
        <FooterPlus
          isDesktop
          footerNavigation={{
            links: [{
              href: 'https://www.portalinmobiliario.com.ar/jobs',
              name: 'Trabaja con nosotros',
              id: 'jobs',
            }],
          }}
          footerLinks={{
            links: [{
              href: 'https://www.portalinmobiliario.com.ar/jobs',
              name: 'Trabaja con nosotros',
              id: 'jobs',
            }],
          }}
          userMenu={{
            links: [{
              href: 'https://www.portalinmobiliario.com.ar/registration',
              name: 'Creá tu cuenta',
              id: 'registration',
            }],
            isUserLoggedIn: false,
          }}
        />
      );

      const renderedComponent = renderer.render(component);

      expect(renderedComponent).toMatchSnapshot();
      expect(renderedComponent.type.name).toEqual('FooterDesktop');
    });
  });
});
