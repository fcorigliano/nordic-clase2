const React = require('react');
const ShallowRenderer = require('react-test-renderer/shallow');
const FooterMobile = require('./mobile');

describe('Portal Inmobiliario Navigation', () => {
  describe('Footer Lite Mobile', () => {
    it('matches the default snapshot for Footer Lite Mobile in desktop', () => {
      const renderer = new ShallowRenderer();

      const component = (
        <FooterMobile
          footerNavigation={{
            links: [{
              href: 'https://www.mercadolibre.com.ar/jobs',
              name: 'Trabaja con nosotros',
              id: 'jobs',
            }],
          }}
          copyright={{
            since: '1999',
            companyName: 'Mercado Libre',
            currentYear: 2019,
          }}
          userMenu={{
            links: [{
              href: 'https://www.mercadolibre.com.ar/registration',
              name: 'Creá tu cuenta',
              id: 'registration',
            }],
            isUserLoggedIn: false,
          }}
        />
      );
      expect(renderer.render(component)).toMatchSnapshot();
    });
  });
});
