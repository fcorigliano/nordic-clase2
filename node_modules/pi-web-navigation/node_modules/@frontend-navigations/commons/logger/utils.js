/**
 * Returns a safe stat path replacing problematic characters
 * @param {string} text Path text to replace unsafe characters
 * @returns {string} Safe stat path
 */
const getPathName = (text = '') => text
  .replace(/[., ]/g, '_')
  .toLowerCase();

module.exports = {
  getPathName,
};
