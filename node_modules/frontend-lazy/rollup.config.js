const { default: babel } = require('@rollup/plugin-babel');

module.exports = {
  input: './loadable.js',
  output: {
    file: './dist/loadable.js',
    format: 'cjs',
  },
  plugins: [
    babel({ babelHelpers: 'bundled' }),
  ],
};
