describe('index', () => {
  test('check create config factory', () => {
    const factoryResult = Symbol('some factory result');
    const factory = jest.fn().mockReturnValue(factoryResult);
    jest.doMock('frontend-config-factory', () => factory);

    const result = require('.');

    expect(result).toBe(factoryResult);
  });
});
