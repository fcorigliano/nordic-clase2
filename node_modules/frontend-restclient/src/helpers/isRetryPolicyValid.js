const { RetryPolicy: BaseRetryPolicy } = require('../policies');

const isRetryPolicyValid = (RetryPolicy) => {
  if (!(new RetryPolicy() instanceof BaseRetryPolicy) || RetryPolicy.hasToRetry === BaseRetryPolicy.hasToRetry) {
    return false;
  }

  return true;
};

module.exports = isRetryPolicyValid;
