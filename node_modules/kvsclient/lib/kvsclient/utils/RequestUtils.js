const _ = require('lodash');

function lowerHeaders(headers) {
  headers = _.transform(headers, (result, val, key) => {
    result[key.toLowerCase()] = val;
  });
  return headers;
}

exports.lowerHeaders = lowerHeaders;
