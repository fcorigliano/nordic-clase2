const HttpStatus = require('http-status-codes');
const KvsError = require('./KvsError');

module.exports = class RejectedExecutionError extends KvsError {
  constructor(message, code) {
    super(message || 'Container over quota', code || 'container_over_quota', HttpStatus.TOO_MANY_REQUESTS);
  }
};
