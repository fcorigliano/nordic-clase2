const _ = require('lodash');
const config = require('./config.json');

function setTestCase(testCaseName) {
  if (_.isNil(config.test)) {
    config.test = {};
  }
  config.test.test_case = testCaseName;
}

exports.setTestCase = setTestCase;
